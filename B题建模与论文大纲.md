# B题建模与论文大纲

本文依据本地《B题 无线电干扰源的快速自动定位与清除》、附件1《模拟器使用说明》、附件2《模拟器通信接口说明及编程指南》和《全国大学生数学建模竞赛论文格式规范（2026年修订稿）》整理。分析采用评阅者关注问题的角度，不代表组委会身份、内部评分标准或官方解答。下文的覆盖布局、选点方法和兜底策略为据题意推导的建议；未运行官方模拟器，未产生演练或正式测试成绩。

建议主线为：**有界测角误差下的集合定位 → 根据定位不确定性主动选点 → 对未知频道进行保证覆盖的搜索 → 将定位、清除和移动统一调度 → 用停止证据判断任务完成。**

## 1 题意拆解与模型边界

| 内容 | 题目给定 | 对建模的影响 |
|---|---|---|
| 目标分布 | 半径1800米圆域，位置固定 | 搜索空间有界；机器狗可到圆域外 |
| 个数与频道 | 问题3、4中10至16个；20个频道；各源频道不同 | 为20个频道分别维护状态；不能找到10个便停止 |
| 接收半径 | 各源独立取值，均在1000至1500米内 | 1000米用于保证覆盖，1500米用于限制已发现源的位置 |
| 测角 | 真方位与示向度误差在正负1度内 | 主模型采用有界误差集合，而非零误差射线 |
| 误差相关性 | 同地点重复检测不会改变误差 | 不能用原地重复测量取平均来声称精度提高 |
| 测量反馈 | 无信号、过近、示向度 | 接口不返回场强、距离、坐标、源类型或真实总数 |
| 移动 | 直线，5米每秒；移动时不能有效检测 | 所有测量都对应离散停靠点，计入真实行走距离 |
| 测量与切频 | 检测5秒，改变检测频道1秒 | 扫频与定位观测均有成本 |
| 清除 | 20米内保证成功；失败3秒，成功5秒 | 清除本身也能获得位置约束 |
| 定向源 | 向未知方向的闭半平面辐射，覆盖角180度 | 无信号可能来自背向，普通圆盘覆盖不再充分 |
| 起终状态 | 原点出发，初始频道1；任意位置退出 | 路径不要求回到原点 |

题目中的道路、障碍物、地形、功率反演和RSSI值均未提供。主模型按题设自由直线移动，不自行添加这些条件。误差分布也未给出；若实验中采用均匀分布、独立噪声或位置先验，必须注明是自建实验假设。

接口读数保留两位小数。数学推导可按1度误差；数值实现可保守使用1.005度并留微小容差，以容纳可能的四舍五入，不能把这一额外裕量写成题目明确给定的噪声。

## 2 统一状态与时间目标

设目标圆域为

\[
\Omega=\{g=(x,y):x^2+y^2\le1800^2\}.
\]

设第\(c\)频道的目标位置、接收半径、定向单位向量分别为\(g_c,R_c,n_c\)。\(R_c\in[1000,1500]\)。用\(e_c\in\{0,1\}\)表示该频道是否存在目标。

机器狗状态至少包括：当前位置\(p\)、当前测向频道\(q\)、虚拟时间\(t\)、现实剩余时间，以及每个频道的观测历史、候选位置集合和处理状态。建议频道状态分为“未知、已发现待定位、已清除、已证明不存在”。

一次测量的虚拟增量为

\[
\Delta T_{\rm measure}=\frac{\|p'-p\|}{5}+\mathbf1(c\ne q)+5.
\]

一次清除的虚拟增量为

\[
\Delta T_{\rm clear}=\frac{\|p'-p\|}{5}+3+2\mathbf1(\text{清除成功}).
\]

清除会改变位置，但不会改变测向机当前频道。若总移动距离为\(L\)、检测次数为\(M\)、切频次数为\(K\)、成功和失败清除次数为\(C_s,C_f\)，则

\[
T=\frac L5+5M+K+5C_s+3C_f.
\]

目标应表达为“在确保全部清除的约束下最小化\(T\)”。实验比较时先比较清除完整性，再比较用时，避免遗漏难找目标却得到较低平均时间。

题目定义的统计量为

\[
\rho=\frac{C_s}{N},\qquad \bar T=\frac{T}{C_s}.
\]

这里的\(T\)包括为确认没有遗漏而做的收尾搜索。\(\bar T\)不是各目标首次清除时刻的平均值；当\(C_s=0\)时应记为未定义。\(N\)只可在演练结束后用于评估，不能作为在线策略输入。

## 3 问题1 误差楔形的交集与定位区域直径

### 3.1 将示向度转成两个半平面

第\(i\)次测量位置为\(s_i\)，示向度为\(\theta_i\)，误差上界为\(\alpha\)。定义

\[
u_i^-=(\cos(\theta_i-\alpha),\sin(\theta_i-\alpha)),\quad
u_i^+=(\cos(\theta_i+\alpha),\sin(\theta_i+\alpha)).
\]

用二维叉积\([a,b]=a_xb_y-a_yb_x\)，误差楔形为

\[
W_i=\{g:[u_i^-,g-s_i]\ge0,\ [u_i^+,g-s_i]\le0\}.
\]

对本题2度开角，这两个约束已经表达朝向目标的前向楔形。三角函数使用弧度；输入角度先转换。采用叉积可避开0度与360度的角度跨界问题。

仅由交会角约束定义的区域为

\[
P=\bigcap_i W_i.
\]

这是凸集，但不保证非空或有界。问题1首先回答这个多边形的直径；在后续应用中，再加入\(\Omega\)、接收半径上界和其他观测约束。圆盘相交后会出现圆弧边界，不应不加说明地仍将其当作有限顶点多边形。

### 3.2 建议实现算法

少量测量时，优先采用易核验的枚举法：

1. 将所有边界写为\(a_jx+b_jy\le d_j\)。
2. 枚举两条非平行边界直线的交点。
3. 检查交点是否满足全部半平面约束；保留可行交点并合并近重复点。
4. 对可行交点求凸包，得到顺序顶点\(v_1,\dots,v_m\)。
5. 枚举顶点对，计算最大平方距离，最后开方。

\[
D(P)=\max_{1\le j,k\le m}\|v_j-v_k\|.
\]

若有\(n\)次测量，枚举交点并检查约束约为\(O(n^3)\)，顶点对枚举为\(O(m^2)\)。数据规模较小时足够实用。高效版本可采用半平面交\(O(n\log n)\)，再用旋转卡壳\(O(m)\)计算凸多边形直径。

论文应证明“直径在顶点对处取得”：对一个端点固定，距离函数在凸多边形上的最大值可由某顶点取得；依次对两个端点应用，便得到顶点对结论。

必须单独处理空集、无界集、单点、线段、近平行边界和重复观测。不能把随意设置的大矩形截断后的直径当作无界交会区域的真实直径。半平面交库通常要求严格内点，退化情形应走单独分支；参见[SciPy官方接口文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.spatial.HalfspaceIntersection.html)。

### 3.3 直径为D的圆不一定能覆盖区域

反例为边长\(D\)的等边三角形：其最小包围圆半径为\(D/\sqrt3>D/2\)。因此，区域直径为\(D\)不足以推出存在半径\(D/2\)的覆盖圆。

这一反例也可以由本题的窄楔形构造：对三条边分别在边的延长线上取足够远的检测点，让一个楔形边界沿该边，另一个边界位于三角形外而不切入三角形。三个有效边界的交集就是该三角形。构造示意图能加强反例与题目的联系。

后续真正应使用的量是最小包围圆半径

\[
r_*(P)=\min_z\max_{g\in P}\|g-z\|.
\]

有限顶点多边形的最小包围圆由至多3个支撑顶点确定，可用两点直径圆、三点外接圆枚举验证，或采用随机增量算法。算法背景参见[CGAL官方最小包围圆文档](https://doc.cgal.org/latest/Bounding_volumes/classCGAL_1_1Min__circle__2.html)。

若\(r_*(P)\le20\)，移动到其圆心就能保证清除。\(D(P)\le40\)只是存在20米覆盖圆的必要条件，一般不是充分条件。一个更保守但简单的充分条件是\(D(P)\le20\)，此时在\(P\)内任取一点都可覆盖\(P\)。

为缩短移动，可进一步求“保证清除且离当前点最近”的位置：

\[
z^*=\arg\min_z\|z-p\|\quad\text{s.t.}\quad\|z-v_j\|\le20,\ \forall j.
\]

这是凸约束优化。只要定位集合由保守外包多边形表示，该解就保留保证性。

建议输出：楔形交会图、直径端点、最小包围圆、等边三角形反例，以及正常和退化输入的算法验证。

## 4 问题2 第二个检测点怎样选

### 4.1 先定义定位效果

已知第一点\(s_1\)和示向度\(\theta_1\)，源位置的保守候选集合为

\[
P_1=\Omega\cap W_1\cap B(s_1,1500).
\]

收到正常示向度时还知道真实距离大于5米。为维持凸外包表示，可以暂时保留这块很小的近距离区域。

建议主指标为新定位集合的最小包围圆半径，辅以面积、直径和移动成本。只用面积有风险：一个面积很小但很长的区域仍无法由20米圆覆盖。

### 4.2 交会角的作用

设两点到源距离为\(r_1,r_2\)，在源处两条视线的夹角为\(\varphi\)。小角度局部近似下，两个误差带交集的面积为

\[
A\approx\frac{4r_1r_2\tan^2\alpha}{|\sin\varphi|}.
\]

它解释了两点近共线时定位变差，以及在距离相近时接近90度交会较有利。但它只是局部近似，不能替代精确楔形相交，也不能忽略第二点失去信号的风险。

单次示向度没有给出源距离，因而“向侧面走固定距离”或“在真实目标附近构造直角”都不是完整的第二点策略。

### 4.3 给出可以计算的候选区域

令\(u=(\cos\theta_1,\sin\theta_1)\)，\(v=(-\sin\theta_1,\cos\theta_1)\)，将第二点写为

\[
s_2=s_1+a u+b v.
\]

对全向源，首次收到信号说明其实际接收半径至少为\(\max(1000,\|g-s_1\|)\)。因此一个严格的保证接收候选集合是

\[
\mathcal C_{\rm safe}=\left\{s_2:\ \|s_2-g\|\le\max(1000,\|g-s_1\|),\ \forall g\in P_1\right\}.
\]

只利用第一条示向度而不利用圆域截断，还可以给出一个特别容易计算的充分子区域：

\[
\boxed{a^2+b^2\le1000^2,\qquad
a^2+b^2\le2000\big(a\cos\alpha-|b|\sin\alpha\big).}
\]

它等价于局部坐标中三个半径1000米圆盘的交集，圆心分别为\((0,0)\)、\(1000(\cos\alpha,\sin\alpha)\)、\(1000(\cos\alpha,-\sin\alpha)\)。这是保守候选区，不是声称最大的可行区。

证明要点：令真实源为\(g=s_1+r(\cos\delta\,u+\sin\delta\,v)\)，\(|\delta|\le\alpha\)。则

\[
\|s_2-g\|^2=r^2+a^2+b^2-2r(a\cos\delta+b\sin\delta).
\]

投影至少为\(a\cos\alpha-|b|\sin\alpha\)。当\(r\le1000\)时，检查关于\(r\)的凸二次函数在0与1000两端，即可保证距离不超过1000；当\(r\ge1000\)时，可保证新距离不超过\(r\)，因此不超过真实接收半径。

例如\((a,b)=(600,400)\)和\((600,-400)\)均满足上述条件，可作为程序调试的初始候选点。它们不是最优点的结论。若到了第二点返回near，则直接清除；保证接收到信号不等于保证一定返回正常示向度。

### 4.4 在候选区内优化

从\(P_1\)取代表位置\(\hat g\)，在候选区左右两侧生成点，排除过短基线和对\(\hat g\)近共线的点。比较每个点的潜在新观测与集合收缩。

可写成最坏情形优化：

\[
\min_{s_2\in\mathcal C_{\rm safe}}
\sup_{g\in P_1,\ |e|\le\alpha}
r_*\!\left(P_1\cap W\big(s_2,\operatorname{atan2}(g_y-s_{2y},g_x-s_{2x})+e\big)\right)
+\lambda\frac{\|s_2-s_1\|}{5}.
\]

式中正常示向度分支用楔形更新，near分支直接清除。\(\lambda\)用于统一精度与时间的权衡，并在演练中校准。问题2可以先取\(\lambda=0\)研究定位质量，问题3、4再加入成本。

实施顺序：粗采样候选点和可能源位置 → 模拟误差情景 → 求交集及包围圆 → 保留若干优良候选 → 局部细化。有限采样得到的是近似选择，不能仅凭网格最优就宣称连续域全局最优或最坏情形已被严格穷尽。

建议输出：候选区图、选点评分热图、与沿示向度前进和纯横向移动的对照，以及交会角和距离变化时的精度曲线。

## 5 问题3 全向源的搜索与清除

### 5.1 用7个点建立发现全部目标的基础方案

选择圆心及半径1500米圆周上的6个等角点：

\[
S=\{(0,0)\}\cup
\{1500(\cos(k\pi/3),\sin(k\pi/3)):k=0,\ldots,5\}.
\]

这7个检测点的1000米接收圆盘能覆盖整个目标圆域。证明如下：半径\(r\le1000\)的目标由圆心覆盖；对\(1000\le r\le1800\)，最近环点与目标的圆心角\(\delta\le30^\circ\)，于是

\[
d^2=r^2+1500^2-3000r\cos\delta
\le r^2+1500^2-3000r\cos30^\circ.
\]

右端关于\(r\)为凸函数，区间最大值在端点，所对应的距离约为807.42米和901.92米，都小于1000米。因此所有全向源必能在至少一个点被发现。

**覆盖证明只证明能发现目标；全部清除还需要后面的定位清除流程。** 7点也不是未经证明的全局最优布局，只是点数少、易实现、有保证的初始方案。

在这些点对每个尚未知的频道至少完成一次足够的覆盖扫描。已发现频道可以转入专门的定位任务；已清除频道无需再扫。全扫最多\(7\times20=140\)次检测，这个数量不包含后续定位观测。

### 5.2 用正负观测更新每个频道的可行集合

全向源正常示向度：

\[
P_c\leftarrow P_c\cap W(s,\theta)\cap B(s,1500).
\]

全向源无信号：如果该源存在且尚未清除，则其位置不能在\(B(s,1000)\)内，因此可据此排除该圆盘。

\[
P_c\leftarrow P_c\setminus B(s,1000).
\]

阴性约束可能使集合非凸或分裂。可以采用几何区域运算，或用自适应网格保守保留候选单元；不能只因一个单元中心被排除就删除整个单元。若同时维护\((g,R)\)，正负观测还可进一步约束同一个固定\(R\)。

### 5.3 单源定位清除模块

1. 收到direction后建立或更新\(P_c\)。
2. 若收到near，原地调用clear。
3. 若存在能覆盖\(P_c\)的20米圆，选择其中移动成本较小的保证清除点。
4. 否则按问题2选下一点，继续交会，或在较近位置获取更窄的角误差带。
5. clear失败后，对已知存在且未清除的源排除\(B(p,20)\)，重新规划。
6. 观测几何长期不改善时，启用有限步光学覆盖兜底，避免死循环。

清除失败仅3秒、测角至少5秒，所以在位置已经较集中的情况下，试探清除可能更省时。需要比较“移动加失败风险的清除成本”和“移动加检测成本”，不应一律等到高度精确定位才行动。

### 5.4 终止条件是本问的重要论证

可以退出的充分条件包括：所有已发现源均成功清除，且对每个未发现频道都已有覆盖整个\(\Omega\)的有效无信号扫描证据。完成上述7点的全频道覆盖并处理全部阳性频道，是一种容易核查的实现。

也可以利用题目上界：成功清除16个不同频道的目标后，已经达到总数上界，可直接结束。清除10个、连续几次无信号或当前没有待处理目标均不足以停止。

### 5.5 缩短用时的改进顺序

先实现7点覆盖基线，再逐项加入：

- **跳过无必要频道**：已清除、已证明不存在的频道不再测量；已发现频道只在有定位收益时测量。
- **同点批量扫频**：当前频道若仍需检测则优先检测它，其余频道顺序主要按任务收益安排。任意两频道切换都是1秒，不存在频道编号距离成本。
- **定位与覆盖复用**：为已发现源选择的定位点，也可对未知频道扫频，减少后续固定巡检点。
- **滚动规划**：在“补足搜索覆盖、定位已发现源、直接清除”三类动作间比较单位成本收益。
- **开放路径优化**：最近邻或最便宜插入产生初始顺序，再用2-opt改进；每次得到新观测后重新规划。不必回到起点。

收益评分可以使用新增覆盖面积、定位包围圆半径下降和预期清除数量，但这些启发式不能删除覆盖与停止保证。建议先用贪心和2-opt形成稳定结果，再考虑更复杂的优化算法。

## 6 问题4 混合源的搜索与清除

### 6.1 修改检测模型

定向源在位置\(s\)能被检测到的条件为

\[
\|s-g_c\|\le R_c,\qquad n_c^\top(s-g_c)\ge0.
\]

\(n_c\)是由源向外辐射的方向。注意\(s-g_c\)与测得的“检测点指向源”的方向\(g_c-s\)相反，不能混用。

无信号观测对应

\[
\|s-g_c\|>R_c\quad\text{或}\quad n_c^\top(s-g_c)<0.
\]

因此不能沿用问题3“无信号就排除1000米圆盘内所有位置”的更新。稳妥做法是维护\((g,R,\beta,\text{类型})\)的联合可行集合或保守近似；角度\(\beta\)必须支持跨0度区间。

若采用粒子或有限网格，它适合动作评分，但“所有粒子被排除”不自动构成不存在的严格证明。不存在判定应使用保守集合、覆盖证书或带界误差的细分方法。

### 6.2 为什么圆盘覆盖还不够

对每个可能源位置\(g\)，取1000米内所有检测点相对于它的向量。要保证任意定向方向都能被至少一点接收，需要

\[
\forall g\in\Omega,\ \forall n\in\mathbb S^1,
\quad\exists s\in S:\ \|s-g\|\le1000,\ n^\top(s-g)\ge0.
\]

一个可核验的充分条件是：\(g\)位于这些邻近检测点的凸包内。因为\(g=\sum_j\lambda_js_j\)且\(\sum_j\lambda_j=1\)，从而\(\sum_j\lambda_j n^\top(s_j-g)=0\)，不可能所有投影都严格为负。

若所有检测点都限制在圆域内，位于边界且朝外辐射的定向源可能只在极特殊的切向位置被接收。附件允许机器狗走到圆域外，应充分利用这一点。

### 6.3 一个具有方向覆盖保证的网格基础方案

在\([-2000,2000]^2\)上取500米间距的正方形网格，共\(9\times9=81\)个节点。圆心在网格上，可直接从原点开始扫描。

任意目标位于某个网格单元内，到该单元4个顶点的距离均不超过

\[
500\sqrt2\approx707.11<1000\text{米}.
\]

目标又位于这4个顶点的凸包中，所以任意180度辐射方向至少覆盖一个顶点。对于目标恰在网格边或节点的退化情形，可同时考虑相邻单元顶点；目标圆域位于网格外边界内部，邻域能够从各侧提供观测点。

对未知频道完成这些节点的扫描，就能保证发现所有全向或定向目标。其作用是提供可运行、有证明的基线，不是建议最终策略机械地扫完1620次。之后可用三角网格、删除冗余节点、已有轨迹复用、自适应补盲和路径优化减少代价；任何删点必须保留位置与方向覆盖证据。

### 6.4 发现定向源后的策略

已有方向观测仍给出正确的源位置楔形，定位几何不变；变化的是第二个检测点不再自动保证能收到信号。问题2的安全区仅适用于全向源。

若从已接收点沿真实的“检测点到源”的线段前进，则距离减小，而且仍在同一个发射半平面内。但实际示向度有误差，不能把沿估计方向走当作严格保证。若下一点失去信号，应将其作为联合约束并调整视角，或转入光学覆盖。

不要把完整识别定向角作为清除的前提。光学清除不受辐射方向限制；只要位置足够确定即可清除。

### 6.5 对全向与定向源均适用的有限步兜底

这是解决“主动定位会不会永远失败”的一个简单构造。

一旦在\(s\)收到正常示向度\(\theta\)，令\(u=(\cos\theta,\sin\theta)\)，\(v=(-\sin\theta,\cos\theta)\)。由于\(r\le1500\)，源必落在局部矩形

\[
g=s+xu+yv,\quad0\le x\le1500,\quad|y|\le h,
\quad h=1500\sin\alpha.
\]

考虑两列光学清除候选点

\[
p_{k,\pm}=s+20k\,u\pm\frac h2v,
\qquad k=0,1,\ldots,75.
\]

任意候选源位置距某点的纵向偏差不超过10米，横向偏差不超过\(h/2\)，因而

\[
\min_{k,\pm}\|g-p_{k,\pm}\|\le\sqrt{10^2+(h/2)^2}.
\]

取\(\alpha=1.005^\circ\)时，右端约为16.52米，小于20米。因此至多尝试152个点，必能清除这个已发现源，不依赖后续无线电观测或定向角识别。

可按两条平行线往返顺序访问，或先访问离当前位置近且覆盖剩余候选区域较多的点；成功即停，失败删除被本次20米圆盘覆盖的候选位置。152是这个保守构造的尝试点数上界，不是平均表现，也不是官方模拟器测得的数据。

它适合作为低频触发的兜底，而非所有目标都采用的主策略。它与有限检测网格共同给出“有限步发现并清除全部目标”的论证；现实20分钟能否完成仍必须用真实接口延迟和演练验证。

## 7 算法结构与伪代码

建议程序拆成五个模块：几何定位、观测状态更新、搜索覆盖、动作选择、接口与日志。这样论文公式能对应到代码实现，出现漏检时也容易定位原因。

```text
读取配置；初始化位置为原点，测向频道为1
调用 enter，保存现实剩余时长
为20个频道初始化状态与候选集合
生成全向覆盖点或混合源覆盖网格

循环：
    如果已清除16个不同频道：退出循环
    如果所有频道均为“已清除”或“有覆盖证据的不存在”：退出循环
    检查现实剩余时间及虚拟时间预算

    生成候选动作：
        对未知频道补足搜索覆盖
        对已发现目标选下一观测点
        在保证清除点或高收益候选点尝试清除
        对长期无进展目标执行有限光学覆盖兜底
    用动作成本和任务收益排序，选择一个动作
    串行发出请求并等待响应

    如果请求未被接受：处理接口错误，保持原物理状态
    如果请求被接受：更新位置、频道、虚拟时间和日志
        direction：添加角度和距离上界约束
        near：安排原地clear
        no_signal：按全向或混合源模型更新
        success：标记该频道已清除
        no_target_in_range：排除20米清除圆盘

接口仍开放时调用 exit
输出自记统计；保存代码版本与测试案例关联
```

现实预算即将耗尽但尚未满足停止证明时，要记录未完成状态，不能为了结束测试把未知频道自动标记为不存在。动作选择还应设置“最多无进展步数”和兜底触发阈值，杜绝死循环。

## 8 演练与结果分析安排

### 8.1 三种验证证据分开写

1. 理论推导：误差楔形包含性、直径算法、包围圆判据、搜索覆盖和有限兜底。
2. 自建算例：固定随机种子，验证算法实现与极端情况；如使用自行设定的位置或噪声分布，明确说明。
3. 官方模拟器：用演练测试比较策略，用问题3和问题4各3次正式测试提交实际结果。

自建仿真不能冒充官方测试。官方不同场次随机生成案例，未提供相同案例重放能力时，不能宣称不同算法是在同一官方案例上作配对比较；配对对照可放在自建固定种子实验中。

### 8.2 建议比较的策略

| 版本 | 内容 | 比较目的 |
|---|---|---|
| 基础版 | 固定覆盖点，逐个目标定位清除 | 建立完整清除的基线 |
| 加主动选点 | 用包围圆缩减指导下一次测量 | 检验定位观测能否减少 |
| 加扫频优化 | 跳过已处理频道，同点批量测量 | 检验检测和切频成本 |
| 加路径优化 | 定位覆盖复用，滚动规划，2-opt | 检验移动距离 |
| 加混合源策略 | 方向覆盖与联合约束 | 检验定向源漏检风险 |

报告每局清除比例、总虚拟时间、平均定位清除时间、现实程序运行时间、移动距离、检测次数、切频次数、失败清除次数、兜底次数和正常退出状态。

在条件允许时，为每类问题安排20至50局演练作为起步，而非只挑表现最好的几局；具体数量取决于实际耗时。全清除率的样本表现与理论保证是两种证据，有限次数全成功不能单独证明任何场景都成功。

### 8.3 必须覆盖的极端场景

- 10个与16个目标；接收半径接近1000米。
- 目标在原点附近、圆域边缘、同一方位、空间聚集。
- 测角误差接近正负1度，两个检测点近共线。
- 定向源位于边界且朝外，检测点在辐射半平面边界附近。
- 第二点返回无信号，clear失败，多次原地相同读数。
- 网络超时后幂等重试，accepted=false，剩余现实时间不足。

### 8.4 建议论文图表

- 楔形、定位多边形、直径与最小包围圆的同图对照。
- 第二点候选区域与评分热图。
- 全向7点覆盖图、混合源网格及边界向外辐射示意图。
- 机器狗轨迹，区别测量点和清除点；真实位置仅能在自建算例中绘制，不能伪称为官方接口提供。
- 虚拟时间的移动、检测、切频、成功清除、失败清除构成图。
- 多局演练的清除比例与平均时间分布；各改进的消融比较。
- 两张正式结果表，每问3行，列为测试案例编码、清除干扰源个数、平均定位清除时间、程序运行时间。

敏感性分析优先改变算法超参数，如选点候选密度、移动权重、观测基线、兜底触发阈值。修改题设速度、误差界或清除半径的结果应单列为模型推广实验，不混入题设成绩。

## 9 接口与测试流程的高频错误

- 只有4个接口：POST /enter、/measure、/clear、/exit；没有独立移动接口。
- 默认地址是本机127.0.0.1的2026端口；先登录模拟器并等待接口就绪。
- 同时检查HTTP状态和accepted。accepted=false时动作没有生效，其virtual_time_s=0不表示时钟归零。
- 每个新动作使用新request_id；网络不确定时，重试原路径、原请求体、原ID，避免重复移动计时。
- 不同新动作不能并发发送。必须收到上一响应后再发送下一动作。
- /clear改变位置但不切频道；只有合法的/measure更新测向机频道。
- 仅在direction时读取svd_deg；near时直接清除；no_signal不等于不存在。
- 虚拟5秒检测不要求现实sleep 5秒；不要人为消耗20分钟程序窗口。
- 使用/enter返回的remaining_real_duration_s控制剩余现实时间，不能一律假定有1200秒。
- 正常完成后主动/exit。结束后接口会关闭，不能再靠/exit查询结果。
- 程序自记请求和响应，以计算时间分解和重建轨迹；官方加密日志另行原样导出。
- 正式测试每问只有3次，手工中止仍消耗机会；它们不能作为调试工具。

题目要求：北京时间2026年9月13日17:30以后不能启动新的演练或正式测试；题目建议15:30之前完成全部正式测试。正式测试启动后仍需完成日志上传，并保留导出的原文件名。问题3和问题4合计应有6份正式测试日志，每份加密日志上传上限为2MB。

## 10 论文写作安排与评阅关注点

可考虑题目：**基于有界测角误差与覆盖搜索的无线电干扰源自动定位清除策略**。最终标题应随实际实现修改，不加入没有实现的算法名称。

建议正文依次组织为：问题分析、模型假设与符号、问题1几何定位、问题2主动选点、问题3全向搜索、问题4混合搜索、实验与误差分析、模型评价与改进、AI工具使用声明、参考文献。附录给出支撑材料清单和完整可运行源程序。以下内容是写作结构建议，不是要求论文增加目录页。

各部分写作重点：

- 摘要用“解决什么问题、采用什么模型、获得哪些实测结果、结论适用范围”组织。问题3、4应填写真实数量与用时；没有测试前不要预填数值。
- 假设只补充必要内容。不能假设知道目标总数、接收半径、真实距离或定向角，也不能把题目已给条件全部重复成创新假设。
- 公式之后解释变量、单位和用途；用流程图连接四问，让问题1和问题2真正服务于后两问。
- 给出能检查的保证：为什么不漏搜，为什么清除点有效，为什么可以退出，异常反馈后如何恢复。
- 清楚标注哪些算法是启发式，哪些结论有证明；一般不能把滚动贪心或遗传算法的结果称为全局最优。
- 从日志验证\(T=L/5+5M+K+5C_s+3C_f\)，说明与接口计时差异是否仅由小数精度造成。
- 缺点应具体，如保守扫描成本、方向状态离散误差、极端场景兜底较慢、候选动作只做有限前瞻，并给出对应改进方向。

从评阅角度，最值得充分说明的是：**测角误差处理正确、未知目标搜索完整、定向盲区处理有效、时间计费准确、代码和正式日志能够支持结论。** 这些是依据题意作出的写作重点，不是官方内部评分权重。

## 11 2026年论文格式与AI使用要求

以下依据本地格式规范及官网现行文件核对：

- A4，四边页边距至少2.5厘米；摘要原则上不超过1页，无需翻译英文。
- 正文不要目录，不超过30页；正文后的附录页数不限。
- 电子论文从摘要页开始，不放承诺书和编号专用页；电子论文单独提交PDF或Word，建议PDF，大小不超过20MB。
- 支撑材料压缩为RAR或ZIP，大小不超过20MB；其清单放在论文附录，附录需包括完整可运行程序。
- 摘要、正文、附录及支撑材料应按匿名要求处理；自写代码和说明避免硬编码个人、学校等信息。官方要求原样提交的加密日志不自行修改或改名。

具体格式以[2026年论文格式规范](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)及适用赛区要求为准。

2026年AI规定要求使用AI的队伍在参考文献之前设置AI工具使用声明，并在支撑材料加入文件名为“AI工具使用详情.pdf”的说明，记录工具与型号、使用环节、主要提示方式，以及采纳、人工修改和核验情况。本次建模讨论属于应如实记录的使用环节。参见[2026年AI工具使用规定](https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html)。

## 12 实施优先级

1. **先打通闭环**：按附件计时例核对接口；实现状态机、日志与near清除，确保程序正常退出。
2. **完成基础几何**：楔形相交、直径、最小包围圆、第二点候选区，检查退化情形。
3. **保证问题3完整性**：7点覆盖加单源定位，加入有限光学兜底；先确保演练完整清除。
4. **保证问题4完整性**：方向覆盖网格，保守处理无信号，避免直接复用全向排除规则。
5. **再优化效率**：先减少检测和移动，再调候选点、扫频安排与兜底触发条件，保留消融结果。
6. **冻结版本后正式测试**：记录代码版本、参数、案例编码与结果表，原样导出6份日志；最后核对论文、程序、统计和日志的一致性。

本大纲中的7点覆盖距离上界、第二点安全候选示例和光学兜底覆盖半径已做数值核对；这些核对仅验证几何构造，不代表已完成官方模拟器演练。
