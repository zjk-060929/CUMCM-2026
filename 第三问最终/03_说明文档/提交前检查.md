# 提交前检查

本交接包是队内材料，`01_最终代码/config.json` 保留队号 `202617241078`，便于
队友复核。若把代码作为论文附件或公开材料提交，必须先进行以下处理：

1. 用 `config.template.json` 替代含真实队号的 `config.json`；
2. 不提交 `02_实测结果/正式测试1` 中含队号和行为明细的原始日志；
3. 检查正文、图片元数据、文件名和压缩包属性中是否残留队号或个人信息；
4. 运行50项离线测试并保存通过截图；
5. 确认正文所述版本名为 `p3_joint_orienteering_spatial_exact2_v6`；
6. 不引用250.859秒/源历史结果作为本版本成绩；
7. 最终提交前重新核对比赛关于代码附件、队号隐藏和文件格式的要求。

