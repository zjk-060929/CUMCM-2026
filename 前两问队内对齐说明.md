# B题第一第二问队内对齐说明

本说明依据当前工作目录中已解压的第一问、第二问材料整理。重点是解释队友实际采用的模型、当前可复现成果、版本差异和需要统一的表述。原有论文、程序、图片和样本文件均未修改；本次复算结果保存在独立临时目录。此处的判断只涉及第一、第二问材料，不代表已经检查第三、第四问实现。

## 1 先明确当前进度

第一问已经形成“有界测角误差 → 半平面交 → 旋转卡壳直径 → 直径圆覆盖判定”的完整解法，有程序、算例、图片和测试。但第一问目录存在明显的交付版本差异：外层成果汇总是修订说明，内层解压分享包仍是旧实现。

第二问已经完成安全候选区证明、连续闭式选点、一阶名义最优性论证、实际观测后的精确角形计算、20,000场离线验证，以及针对审阅意见的修订。推荐动作始终为相对首次示向度左偏或右偏约35.10度，直线移动1000米，再检测同一频道。修订没有改变这个闭式点，主要补强了结论范围、几何退化处理和边界检验。

前两问目前交付的是数学与离线程序成果，不是官方模拟器成绩，也不是多目标全清除控制程序。

### 1.1 文件与版本矩阵

| 当前文件位置 | 实际内容 | 本次核验 | 建议用途 |
|---|---|---|---|
| [第一问外层成果汇总](/Users/kkkw/Desktop/国赛/第一问/第一问成果汇总_队友版.md) | 修订算法说明、合法测向反例、退化情况、声称21项测试 | 文档已读；对应完整新代码包未找到 | 理解第一问修订思路，但不能据此认定当前代码全部同步 |
| [第一问解压分享包说明](/Users/kkkw/Desktop/国赛/第一问/B题第一问_队友分享包_20260911/README.md) | 旧版排序与双端队列半平面交、4幅图、6项测试 | 6项测试通过；合法单点与线段仍抛异常 | 已交付的旧版可运行基线 |
| [第二问第一版说明](/Users/kkkw/Desktop/国赛/第二问/question2第一版/README.md) | 原闭式点、旧几何代码、3组图、7项测试 | 7项通过 | 追溯修改历史，不作为最新文字口径 |
| [第二问审阅报告](/Users/kkkw/Desktop/国赛/第二问/第二问审阅与修改建议.md) | 对原稿的审阅、独立反例和修改建议 | 与修订版逐条对照 | 理解为什么修订；其中部分相对文件链接已过时 |
| [第二问修订版说明](/Users/kkkw/Desktop/国赛/第二问/B题第二问_修订队友版_20260911/README.md) | 修订正文、代码、4组图、7页Word/PDF、19项测试、20,000条样本 | 19项通过，全部样本已重新计算 | 当前第二问讨论和并稿的主要依据 |

### 1.2 本次实际验证了什么

| 项目 | 结果 |
|---|---|
| 第一问现有分享包测试 | 6/6通过 |
| 第二问第一版测试 | 7/7通过 |
| 第二问修订版测试 | 19/19通过 |
| 修订版20,000个场景重新生成并计算 | 全部完成 |
| 与修订版保存CSV的直径最大差 | 约1.69×10⁻¹²米 |
| 与保存CSV的最小包围圆半径最大差 | 约1.34×10⁻¹²米 |
| 第二点有信号、集合有效、包含真实源 | 本批样本均为100% |
| near、无信号、无效交集的样本数 | 均为0 |
| 第一问270度覆盖与269度不覆盖算例 | 均可复算 |
| 第一问外层文档的单点、线段算例 | 旧分享包失败，第二问修订版几何程序成功 |

微小数值差异属于浮点运算末位差异，不影响所报告统计精度。测试通过不能替代数学证明，也不意味着尚未覆盖的异常情形均已完善。

## 2 两问究竟在解决什么不同问题

第一问是“已经给了观测，怎样求位置范围和范围大小”。它是几何计算模块。

第二问是“现在只有一次观测，下一次去哪里观测才更好”。它是观测位置决策模块。

两者衔接为：

首次位置与示向度 → 第二问生成第二检测点 → 获得第二次真实读数 → 第一问几何模块求角度可行域 → 求直径与最小包围圆 → 交给后续搜索清除模块。

这里必须区分三类位置：检测点、未知干扰源位置、将来尝试清除的位置。第二检测点既不是估计出的源位置，也不是保证成功的清除点。

## 3 第一问思路的完整解释

### 3.1 为什么不能直接求两条中心线的交点

示向度有正负1度误差。如果读数为0度，真实方向可以在359度到1度之间。实际约束是一块朝向前方的窄角形，而不是一条无误差直线。

同地点重复读数的误差固定，因此同一个位置重复测十次，不会把这块角形缩小十倍。队友据此使用确定性区间约束，而不使用独立高斯噪声平均。

### 3.2 把角形写成半平面

第i次检测位置为Sᵢ，读数为θᵢ，误差半角α=1°。令

\[
u_i^-=(\cos(\theta_i-\alpha),\sin(\theta_i-\alpha)),\quad
u_i^+=(\cos(\theta_i+\alpha),\sin(\theta_i+\alpha)).
\]

对候选源位置X，用二维叉积a×b=aₓbᵧ−aᵧbₓ写成

\[
u_i^-\times(X-S_i)\ge0,\qquad
u_i^+\times(X-S_i)\le0.
\]

直观理解是：X应在下边界左侧、上边界右侧。把上边界方向反转，就能统一成“位于每条有向直线左侧”。这种向量写法自动处理0度/360度跨界。

所有观测联合起来就是

\[
P_{\rm ang}=\bigcap_iW_i.
\]

半平面是凸集，交集仍是凸集。非空、有界且不退化时是凸多边形；也可能是单点、线段、空集或无界区域。

队友当前统一采用“纯角度可行域”口径：这里不裁剪半径1800米源域，不裁剪1500米接收上界，也不去掉距离不超过5米的近距离部分。这样保留纯多边形模型，便于第一、第二问结果一致。它是物理可行集合的保守外包，区域无界只表示角度信息不足，并不表示真实源可以出目标圆域。

### 3.3 当前材料实际出现了三种半平面交实现

| 位置 | 方法 | 退化处理与复杂度口径 |
|---|---|---|
| 第一问解压分享包、第二问第一版 | 方向排序、删同向冗余线、双端队列维护有效边界 | 拒绝单点/线段；核心算法可为O(n log n)，但当前代码还有逐顶点逐约束复核，完整最坏复杂度不能直接写成该阶 |
| 第一问外层修订汇总 | 每条边界对所有约束求参数区间，再取端点凸包 | 文档写明支持完整分类，O(n²)；对应源代码未在当前目录找到 |
| 第二问修订版内的q1_localization.py | 枚举两两边界交点、验证全部约束、去重和凸包 | 已支持点/线段，文档和核心实现为O(m³)，m为半平面数 |

因此，“第一问已经修订”“第二问已经支持退化”可以同时成立，但仍不能说两个目录已经采用同一个程序。

第一问外层修订方法可这样理解。对第j条边界令

\[
X(t)=p_j+t d_j.
\]

代入第k个半平面，得到

\[
(d_k\times d_j)t+d_k\times(p_j-p_k)\ge0.
\]

这就是对一个实数t施加上下界。把所有约束合并，可以判断这条边界的哪一段、哪条射线或哪一点仍然可行。再收集所有有限端点取凸包。这个思路能自然保留单点、线段和无界情况，是修订稿相较旧版的主要改进方向。

### 3.4 为什么只求顶点之间的最远距离

区域直径定义为

\[
D(P)=\max_{X,Y\in P}\|X-Y\|.
\]

如果区域顶点为V₁,…,Vₖ，则

\[
D(P)=\max_{i,j}\|V_i-V_j\|.
\]

原因是区域内的点都是顶点的凸组合。两个凸组合之差仍能写成顶点差的加权和；三角不等式说明它的长度不会超过最远顶点对的距离。

直接枚举顶点对是O(k²)。队友使用旋转卡壳：沿多边形依次遍历一条边，并维护离这条边最远的支撑顶点。随着边旋转，这个顶点索引也单调前移，所以在凸包已经给出的前提下，求直径为O(k)。平行边导致支撑面积相等时，还要比较相邻顶点；当前卡壳代码包含这一分支。

旋转卡壳负责的是“最远点对”，不负责求半平面交，也不负责求最小包围圆。

### 3.5 直径圆为什么要单独检验

设最远点对为A、B，D=‖A−B‖。以AB为直径的圆盘为

\[
C=(A+B)/2,\qquad R_D=D/2.
\]

覆盖的充要条件是

\[
\max_i\|V_i-C\|\le R_D,
\]

也等价于对每个顶点检查

\[
(V_i-A)\cdot(V_i-B)\le0.
\]

圆盘是凸集，所以包含全部顶点就包含它们的凸包。

补充一条对齐时有用的逻辑：若一个半径D/2的圆包含距离恰为D的A、B，其圆心必定是AB中点。因此，检验这个圆不仅是在试一个任意圆心；检验失败就说明不存在同样半径的其他圆来完成覆盖。

### 3.6 队友已经给出的两类反例

第一类是等边三角形：边长为D时，第三顶点到任意一条边中点的距离为√3D/2，大于D/2，所以直径圆不覆盖。其最小包围圆半径为D/√3，也大于D/2。

第二类是符合题目测向规则的269度反例，更适合正文展示：

| 参数 | 覆盖算例 | 不覆盖算例 |
|---|---|---|
| 第一检测点与读数 | (0,0)，0° | 相同 |
| 第二检测点 | (1000,1000) | 相同 |
| 第二读数 | 270° | 269° |
| 区域直径 | 49.385425824米 | 49.362859759米 |
| 直径圆半径 | 24.692712912米 | 24.681429880米 |
| 是否覆盖 | 是 | 否 |
| 不覆盖的程度 | 仅浮点末位偏差 | 一个顶点比半径超出0.434511145米 |

两组结果本次均已复算。269度反例可以选择约(982.544935,0)作为真实源，其到两检测点距离约982.545米、1000.152米，选择1200米接收半径便满足题意。

值得理解的是：不覆盖算例的区域直径还略小，但仍不能把区域装进直径圆。这说明“直径更小”和“该直径圆覆盖”是不同命题。

### 3.7 退化情况是合法结果

外层汇总给出的两组测向能分别把P约束成原点、或者一条长度约3.491012986米的线段。它们都可以对应原点真实源，并满足每条读数误差不超过1度。

因此不能把点或线段一概当成数据矛盾。现有第一问分享包在这两组输入上仍抛出“定位区域退化为点或线段”；第二问修订版程序则分别返回直径0和3.491012986米。

### 3.8 第一问当前最重要的交付缺口

外层汇总列出了counterexample_input.json、point_input.json、segment_input.json、example_results.json、修改说明.md以及fig5_bearing_counterexample.png。这些文件在当前第一问目录未找到。图1至图4虽存在于内层旧分享包，但不代表它们已经按外层修订流程更新。

建议对齐时要求队友提供“与外层修订汇总完全配套的一整个目录”，再统一源码、21项测试、5幅图、输入例子和文档。不要只复制一个新汇总文件，然后继续使用旧包中的源程序。

## 4 第二问思路的完整解释

### 4.1 最终动作先说清楚

令首次位置为S₁，首次读数方向单位向量为u，左法向为v。队友最终选点为

\[
\boxed{S_2=S_1+\frac{9000}{11}u\pm\frac{2000\sqrt{10}}{11}v.}
\]

也就是局部坐标

\[
(a,b)=(818.1818,\pm574.9596)\ \mathrm m.
\]

实际动作是左偏或右偏35.0968度，沿直线走1000米。两个分量不是两段路。频道不变，从第一次检测结束计到第二次检测结束：

\[
T_2=1000/5+5=205\ \mathrm s.
\]

若真的先前进818.18米再横移574.96米，路程将变成1393.14米，时间约283.63秒。这是第一版文字错误，修订版已纠正。

左右两个点在孤立、对称的第二问模型下等价。如何结合其他目标、当前位置之后的路径、已有观测选择一侧，属于后续任务规划，当前代码只是按side输出左、右或两侧。

### 4.2 为什么队友采用分层目标

队友没有给“定位误差、时间、信号风险”随意设权重，而是：

1. 把第二次仍能收到全向源信号作为硬约束。
2. 在这个候选区中最小化名义最坏定位直径。
3. 把205秒作为单独的时间指标，供后续问题评估。

这使选点能够用连续公式解释，避免坐标随网格步长或经验权重改变。但因此也不应把这个点称为整场任务总时间最优。

### 4.3 局部坐标的作用

取

\[
u=(\cos\theta_1,\sin\theta_1),\quad
v=(-\sin\theta_1,\cos\theta_1),\quad
S_2=S_1+au+bv.
\]

这样无论第一次位于哪里、朝向多少度，都可以先在“原点、正东方向”的统一坐标中推导，然后旋转和平移回去。

例如第一次在(10,20)、示向度90度时，左侧第二点为

\[
(10-574.9596,\ 20+818.1818).
\]

这项坐标旋转已经在测试中覆盖。

### 4.4 安全候选区的推导

设真实源相对第一次位置的距离为r，真实方向相对读数偏差为δ，|δ|≤α。则

\[
G=S_1+r(\cos\delta\,u+\sin\delta\,v),
\]

\[
d_2^2=\|S_2-G\|^2=r^2+a^2+b^2-2r(a\cos\delta+b\sin\delta).
\]

令q=a²+b²，η=a cosα−|b|sinα，则

\[
d_2^2\le r^2+q-2r\eta.
\]

两条充分条件为

\[
\boxed{q\le1000^2,\qquad q\le2000\eta.}
\]

对r≤1000，上式右侧是关于r的凸二次函数，两个端点都不超过1000²，所以第二点仍在最小接收半径内；对r≥1000，第二条条件说明d₂≤r，而第一次已经能接收，因此第二次也能接收。

这里最关键的逻辑是“源自己的接收半径至少为max(1000,r)”，不是假设所有源接收半径固定为1000米或1500米。

该候选区是三个半径1000米的圆盘交集。若写成移动长度L和偏转角β，则充分条件可表达为

\[
L\le1000,\qquad L\le2000\cos(|\beta|+\alpha).
\]

特别地，当L=1000时，允许的偏转角满足|β|≤59度。队友的35.0968度在其中。这一角度表达是对原不等式的等价解释，便于理解候选图。

这是充分安全域，不宣称等于结合全部源域和距离信息后的最大可行区域。原点虽然满足非严格安全条件，但不作为有效第二点；实际选点还需要非零横向分量形成交会。

### 4.5 接收安全和定位最优使用了不同层级的模型

安全证明考虑首次误差范围内所有可能真实方向；而闭式最优性为了可以解析求解，暂时把源放在首次读数中心线上，即G=(r,0)，并用一阶误差带近似两块角形。

所以不能把前者的全方向安全保证，自动传递成后者的全误差定位最优保证。修订版已经明确区分了这两件事。

### 4.6 一阶模型具体是什么

在名义源附近，用宽度近似固定的平行误差带代替张开的角形。两条误差带的交集是平行四边形；其两条半对角线长度为

\[
h_1^2=\tan^2\alpha\left[r^2+\frac{(L^2-ar)^2}{b^2}\right],
\]

\[
h_2^2=\tan^2\alpha\left[r^2+\frac{(2r^2-3ar+L^2)^2}{b^2}\right].
\]

因此近似直径为

\[
D_{\rm lin}(r;a,b)=2\max(h_1,h_2).
\]

可以从一阶方位模型解释这些式子。名义源附近的位置偏差为(Δx,Δy)，第一条误差带约束|Δy|≤r tanα；第二条带约束

\[
|b\Delta x+(r-a)\Delta y|\le d^2\tan\alpha,
\quad d^2=(r-a)^2+b^2.
\]

将两条带各自取正负边界，求出平行四边形顶点，就得到上述两组半对角线。分母b²也直观反映了横向基线太小时的不良几何。

这一近似忽略了角形顶点及远离名义点时宽度随距离变化的细节，尤其在近距离情况下可能不准确。实际定位程序随后恢复精确角形求交，而不是用这个近似公式替代所有计算。

### 4.7 为什么最坏名义距离是1500米

有

\[
h_2^2-h_1^2=
\frac{4r(r-a)((r-a)^2+b^2)\tan^2\alpha}{b^2}.
\]

当r<a时h₁较大，且h₁在这段内递减；当r>a时h₂较大，且h₂在这段内递增。因此整段最大值只需比较左右端点。利用a≤1000和r最大1500，右端点不小于左端点，所以名义最坏位置是r=1500。

这里“最坏”仅对中心线上的径向位置取最大，不是在所有第一次、第二次误差组合中取最大。

### 4.8 为什么最后恰好得到1000米与9/11

令rₘₐₓ=1500，记为R；写a=Lx、|b|=L√(1−x²)，则目标可以化成

\[
\frac{J_D^2}{4\tan^2\alpha}=R^2+\frac{(A-Bx)^2}{1-x^2},
\quad A=\frac{2R^2}{L}+L,\ B=3R.
\]

固定L，对x求导：

\[
\frac{\mathrm d}{\mathrm dx}\frac{(A-Bx)^2}{1-x^2}
=\frac{2(A-Bx)(Ax-B)}{(1-x^2)^2}.
\]

在放宽后的方向区间0<x<1内，唯一极小点是x₀=B/A。代回去，得到固定长度L下可达到的目标下界

\[
g(L)=\left(\frac{2R^2}{L}-L\right)^2.
\]

它在0<L≤1000上严格递减。因而任何L<1000的安全点都不可能优于L=1000的下界。再验证L=1000时达到下界的点也满足原安全条件，就证明了它确实是限定优化问题的最优点。

代入R=1500、L=1000：

\[
x^*=\frac{3RL}{2R^2+L^2}=\frac9{11},\quad
a^*=\frac{9000}{11},\quad |b^*|=\frac{2000\sqrt{10}}{11}.
\]

最优名义一阶直径为

\[
J_D^*=7000\tan1^\circ\approx122.185454\ \mathrm m.
\]

这是连续推导所得，不是随机搜索、拟合或网格碰巧得到。q2_theory.py将推导结果写成确定公式，在线给定S₁、θ₁就能直接输出S₂。

## 5 第二问实际定位与清除判据

收到第二次实际读数后，计算

\[
P_{\rm ang}=W_1\cap W_2.
\]

程序枚举四条边界的交点，检查可行性并构造凸包，再用第一问方法求直径。这里“精确”指不采用一阶误差带近似；实现仍是双精度浮点运算，存在明确容差，并不是符号精确算术。

候选集合可能为四边形、三角形、线段或单点，并非四条约束必有四个有效顶点。最小包围圆在q2_validation.py中另行计算：枚举单点、两点直径圆和三点外接圆，选能覆盖全部顶点的最小圆。对两次观测产生的少量顶点，这个方法很直观。

最小包围圆为

\[
\rho(P)=\min_z\max_{X\in P}\|X-z\|.
\]

若ρ≤20，移动到圆心后可以保证清除；只知道D≤40一般不够。当前样本里D≤40和ρ≤20的比例恰好一致，不代表二者对任意定位多边形等价。

修订正文还提出可以利用

\[
F_2=\Omega\cap W_1\cap W_2\cap B(S_1,1500)\cap B(S_2,1500)
\]

进一步缩小源位置范围。但该集合包含圆弧边界，当前统计均没有改用这一口径。这属于已提出的扩展，不能写成现有20,000个结果已经融合这些圆域约束。

## 6 第二问的数值到底证明了什么

### 6.1 四个容易混淆的直径

| 数值 | 含义 | 可以支持的结论 |
|---|---|---|
| 122.185454米 | 中心线名义源、一阶误差带、全部径向距离上的最坏值 | 限定模型的理论结果 |
| 122.462636米 | G=(1500,0)，两次零误差，真实角形交会直径 | 特定名义场景的精确核验 |
| 127.487346米 | 20,000个自设随机场景里的最大值 | 有限样本最大值 |
| 135.184560米 | 首次真实方向+1度、第一误差−1度、第二误差+1度的连续读数场景 | 一个误差边界反例；尚未证明为全局最大值 |

第一和第二个值相对差约0.2263%，只适用于该1500米名义场景。第四个值比第一个约大10.64%，因此122.185454不能当作全误差范围的实际直径上界。

还有一个接口衔接细节：135.184560场景使用未舍入的第二读数322.160771571度；官方接口读数只保留两位小数。因此它应称为连续误差数学验证。若改为两位小数322.16度，同一场景本次复算为135.179923571米，第二误差约0.999228429度，仍足以否定122.185454的全误差上界说法。接口量化应另行核验，不能把未舍入读数直接当作官方返回记录。

### 6.2 近距离反例

令G=(10,0)，两次实际误差为0。修订版计算得到：

- 精确角形交集为三角形。
- 精确直径40.104270米。
- 一阶近似直径60.221770米。

这表明一阶近似在某些近距离场景误差明显，不能从“1度很小”直接推出所有场景误差都只有0.23%。两条误差带模型在源附近的局部近似性质和角形顶点限制，是造成差别的原因。

### 6.3 35.10度与45度比较的范围

在同一个G=(1500,0)、两次零误差的名义场景：

- 当前点的精确直径为122.462636米。
- 45度安全点的精确直径为126.116408米。
- 两者都移动1000米、耗时205秒。

因此当前点在这一场景的直径更小约2.90%。这项比较已经完成，但多个策略在同一批20,000场景中的全面对照尚未交付，不能据此宣称在所有场景都优于45度。

### 6.4 20,000场随机验证怎样生成

随机种子为20260921。先取接收半径R_c在[1000,1500]均匀分布；在首次能收到正常示向度的条件下，用

\[
r=\sqrt{5^2+U(R_c^2-5^2)},\qquad U\sim U[0,1]
\]

生成径向距离。这相当于给定接收半径后的面积加权径向采样，不是把r本身设为均匀分布。

误差通过“场景种子加位置坐标”的确定性哈希产生，范围在正负1度内；同地点重复检测误差不变。每个场景再设真实方向为首次误差的相反数，使首次读数固定为0度。第二点因此可统一用左侧闭式点计算。

这是一套自建离线分布，不是题目声明的真实分布，也不是在整个目标圆域、任意首次位置和所有可能误差场上的穷尽。安全证明独立于它；样本用于检查固定策略和程序表现。

### 6.5 统计结果及解释

| 指标 | 当前保存与复算结果 |
|---|---:|
| 定位直径均值 | 49.393725米 |
| 中位数 | 43.519400米 |
| P90 | 73.731788米 |
| P95 | 86.396460米 |
| P99 | 104.799087米 |
| 样本最大值 | 127.487346米 |
| 最小包围圆半径均值 | 24.696926米 |
| 第二点保留信号 | 20,000/20,000 |
| 有效集合且包含真实源 | 20,000/20,000 |
| 直径圆能够覆盖定位区域 | 19,605/20,000=98.025% |
| 最小包围圆半径≤20米 | 7,581/20,000=37.905% |
| 直径圆覆盖且半径≤20米 | 7,186/20,000=35.93% |

37.905%只表示这套自设分布中，两次测向后的纯角度集合可以装入20米圆的比例。它不是实际清除率：程序还没移动到清除点、没有执行官方clear，也没有处理多目标搜索与终止问题。

本次重算20,000场景与保存文件的数值一致到约10⁻¹²米。修订前后原保存数据也已直接比较，直径最大差4.69×10⁻¹³米，最小包围圆半径最大差5.57×10⁻¹²米。这支持“修订没有改变原有非退化样本结果”。

所有随机样本的反馈分支均为direction；不能把这组实验当成near、no_signal、HTTP错误或重试等分支的实际验证。当前闭式点远离首次狭窄角形的近距离区域，near分支本来也不容易由该构造触发。

## 7 修订版相对第一版实际改了什么

| 内容 | 修订情况 |
|---|---|
| 818.18、574.96的坐标 | 保持不变 |
| 闭式理论程序核心数值计算 | 保持不变，主要修订说明和目标名称 |
| “先前进再横移” | 改为一次斜向直线移动 |
| 205秒 | 明确为第一次检测结束后、同频道的移动加第二次检测 |
| 最优性 | 明确限定充分安全域、名义中心线、一阶模型、径向最坏目标 |
| 最优长度证明 | 修订正文用放宽方向后的目标下界，再验证原安全域可达，论证更完整 |
| 半平面交代码 | 从旧双端队列改为交点枚举、可行性检查和凸包 |
| 单点与线段 | 已在第二问修订版支持并加入测试 |
| 交集形状 | 不再一概称为四边形 |
| 边界场景 | 新增135.184560米连续读数反例 |
| 近距离场景 | 新增10米三角形反例 |
| 候选点比较 | 新增一个名义场景下35.10度与45度比较 |
| 图像 | 从3组增加到4组，PNG和SVG齐全 |
| 自动化测试 | 从7项增加到19项 |
| 展示文档 | 增加7页Word和PDF队友版 |
| 官方模拟器 | 本包仍未对接，也未形成官方测试成绩 |

修订落实清单中的“补充候选比较”是部分落实：确定性单场景已完成，审阅报告提出的多策略同批随机场景对比尚未全面完成。几何程序的退化能力也确实提高，但实现方式并非第一问外层汇总记载的逐边界区间法。

## 8 当前仍需对齐或补齐的事项

### 8.1 第一优先是版本闭环

需要确认第一问最终采用哪一个程序，并让正文中的算法步骤、复杂度、测试数量和图片全部与之对应。现有材料不能混用“O(n log n)核心队列”“O(n²)边界区间法”“O(m³)交点枚举法”这三种说明。

建议最终只保留一个公共定位模块供第二、第三、第四问调用，避免多处同名q1_localization.py继续分叉。若分别保留验证用参考实现，应在文件名和说明中标清用途。

### 8.2 不宜把当前几何模块当作完整异常分类器

本次另行复现了一个通用半平面入口的问题：对x≥1和x≤0这种明显空集，第二问修订程序会报告“无界”。原因是它先检查是否存在非零衰退方向，再检查可行点；但判断非零方向能导致无界，必须以集合非空为前提。

这不推翻现有20,000个有效样本结果，却说明空集和无界的区分尚未完全实现。它也进一步证明第二问修订程序不能冒充第一问外层汇总中声称具备完整状态分类的那一版。

### 8.3 理论范围和实际清除范围

必须统一三句话：

1. 第二点对全向源的接收安全已有连续证明。
2. 35.10度点的最优性仅在所定义的名义一阶模型下成立。
3. 两次定位后能否直接清除，另由最小包围圆或更精确的物理可行集合决定。

不能因为第二点移动时间固定为205秒，就认定它在第三问里对每个目标都最省时间；也不能直接应用于定向源，因为移动后仍在接收距离内不代表仍在辐射半平面内。

### 8.4 后续有价值但尚未完成的验证

- 对多个安全候选点使用相同场景、相同地点误差场做系统对照，报告均值、分位数、边界值和时间。
- 若需要更强结论，再研究全部误差组合下的精确最坏直径和对应选点，不能以已有样本最大值替代。
- 验证官方两位小数读数下的集合包含性和容差处理。
- 完成源域、接收距离约束的实际集合裁剪，或明确继续使用纯角度保守模型。
- 把几何结果接入真实动作状态机，补上移动、检测、清除、频道保持和终止逻辑。

这些是当前完成范围之外的工作，不是证明前两问现有解法失败的必要条件。问题2原题也没有要求第二次测量后一定直接清除源。

## 9 你可以怎样向队友复述

“第一问已经把测角误差变成角形约束，用半平面交求定位范围，用旋转卡壳求直径，再单独判断直径圆是否覆盖。不能默认直径圆覆盖，也不能把合法的点或线段当成矛盾。目前第一问新汇总和旧分享包还没对齐，需要补齐与21项测试对应的新代码及图。”

“第二问在这个几何评价基础上，先证明一个不会丢失全向信号的安全候选区，再用中心线名义源的一阶误差带模型，对未知距离做最坏情形优化，解析得到左或右偏35.0968度、直走1000米的点。从第一次测量结束到第二次测量结束为205秒。拿到真实读数后，仍用角形求交算真实的候选集合。”

“122.1855是名义一阶理论值，122.4626是特定零误差精确值，127.4873是随机样本最大值，135.1846是连续误差边界反例。它们不能混写。20,000场离线结果可以复现，37.905%是20米圆覆盖比例，不是正式测试清除率。下一步优先统一几何模块和文字口径，再决定哪些更强验证值得补。”

## 10 对齐会议可以直接逐项确认的内容

| 需要确认的事项 | 会议希望形成的结果 |
|---|---|
| 第一问最新代码包在哪里 | 提供与外层汇总相符的源码、21项测试、输入文件和5幅图 |
| 全文使用哪套几何程序 | 明确唯一公共版本，或区分正式实现与独立验证实现 |
| 第一问和第二问定位范围是否都是纯角度口径 | 明确P_ang与加入圆域后的F₂，保证表格没有混用 |
| 35.10度点的最优性怎么写 | 统一限定表述，不使用无条件全局最优 |
| 205秒从何时起计 | 第一次测量完成后，同频道，1000米直线移动加第二次测量 |
| 四个直径数值怎样引用 | 标明理论、名义精确、随机最大、边界反例 |
| 哪些实验真正执行过 | 区分本包离线测试与未来官方演练、正式测试 |
| 如何接入后续问题 | 约定几何模块输出、异常状态、最小包围圆和后续动作选择 |

阅读顺序建议：先读本说明第1节，再读第一问外层成果汇总，接着读第二问修订版思路讲解和论文正文，最后看仿真摘要与审阅落实清单。程序同学再重点检查几何版本和接口数据结构，论文同学重点统一公式、复杂度、数字含义和图号。
